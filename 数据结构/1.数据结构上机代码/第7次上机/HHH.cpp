#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <iomanip>

using namespace std;

int main()
{
    int n,m;
    while(~scanf("%d%d",&n,&m))
    {
        if(n==m==0)
            break;
        else if(n>m)
        cout<<""
    }
}
