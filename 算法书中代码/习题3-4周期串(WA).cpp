#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <cstring>

using namespace std;
int findindex(string s)
{
    int len=s.size();
    if(index==len)
}
int main()
{
    string s;
    while(cin>>s)
    {
        int len=s.size();
        char ch=s[0];
        int index;
        for(int i=0;i<len;i++)
        {
            if(s[i]==ch)
                index=i;
        }
        char[index];
        for(int i=0;i<index;i++)
        {
            char[i]=s[i] ;
        }
        for(int i=0;i<len;i++)
        {
            if(s[i]!=char[i%index])
                index=
        }
    }
}
